@page
@model _17nsj.Jedi.Pages.DocumentListModel
@{
    ViewData["Title"] = "資料";
}

<section class="content">
    <div>
        <div class="box box-primary">
            <div class="box-header with-border">
                @if (Model.IsSysAdmin)
                {
                    <a href="/DocumentManage" type="button" class="btn btn-primary pull-right"><i class="fa fa-plus"></i></a>
                }
            </div>
            <div class="box-body">
                <ul class="products-list product-list-in-box">
                    @foreach (var item in Model.ドキュメントリスト)
                    {
                        <li class="item">
                            <div class="product-img">
                                <img src="@item.ThumbnailURL" alt="Product Image" style="height: 70px; width: 70px; margin-left: 5px; margin-right: 5px;">
                            </div>
                            <div class="product-info">
                                <h4 class="product-title" style="margin: 0px">
                                    <a href="@item.URL" target="_blank">@item.Title</a>
                                </h4>
                                <span class="product-description">
                                    @item.Outline
                                </span>
                                @if (Model.IsSysAdmin)
                                {
                                    <a href="/DocumentManage?id=@item.Id" type="button" class="btn btn-info btn-flat">編集</a>
                                    <a href="/DocumentDelete?id=@item.Id" type="button" class="btn btn-danger btn-flat">削除</a>
                                }
                            </div>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </div>
</section>
