@page
@model _17nsj.Jedi.Pages.DocumentManageModel
@{
    ViewData["Title"] = "資料管理";
}

<section class="content">
    <div>
        <div class="box box-primary">
            <div class="box-body">
                <div class="col-md-8 col-md-offset-2">
                    @using (Html.BeginForm(FormMethod.Post))
                    {
                        @Html.HiddenFor(model => model.IsEditMode)
                        @Html.HiddenFor(model => model.TargetDoc.UpdatedAt)
                        <div class="form-horizontal">
                            <div class="box-body">
                                @if (Model.IsEditMode)
                                {
                                    <div class="form-group">
                                        <label class="col-sm-2 control-label">ID</label>

                                        <div class="col-sm-10">
                                            <label class="form-text">@Model.TargetDoc.Id</label>
                                            @Html.HiddenFor(model => model.TargetDoc.Id)
                                        </div>
                                    </div>
                                }
                                <div class="form-group">
                                    <label class="col-sm-2 control-label">タイトル*</label>

                                    <div class="col-sm-10">
                                        @Html.TextBoxFor(model => model.TargetDoc.Title, new { @class = "form-control", @placeholder = "30文字以内" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="col-sm-2 control-label">サムネイルURL*</label>

                                    <div class="col-sm-10">
                                        @Html.TextBoxFor(model => model.TargetDoc.ThumbnailURL, new { @class = "form-control", @placeholder = "200文字以内" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="col-sm-2 control-label">URL*</label>

                                    <div class="col-sm-10">
                                        @Html.TextBoxFor(model => model.TargetDoc.URL, new { @class = "form-control", @placeholder = "200文字以内" })
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="col-sm-2 control-label">説明*</label>

                                    <div class="col-sm-10">
                                        @Html.TextAreaFor(model => model.TargetDoc.Outline, new { @class = "form-control", @placeholder = "100文字以内" })
                                    </div>
                                </div>
                            </div>
                            <div class="box-footer">
                                <button type="submit" class="btn btn-success pull-right">登録</button>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</section>